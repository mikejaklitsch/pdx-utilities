[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "pdx-utilities"
version = "1.0.0"
description = "Shared utilities for PDX modding tools"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["pdx_utilities*"]
